# Filled in by Gradle at build time - see processResources in build.gradle.kts.
# Kept apart from application.properties because that one is hand-edited: this file is run through
# a template engine, where a stray dollar sign is a build failure rather than a character.
version=${version}
