# CLAUDE.md

このリポジトリで作業するときに守る制約をまとめた指示書です。プロジェクトの説明や使い方は
[README.md](README.md) にあります。ここには **README の要約ではなく、逸脱してはいけない規約だけ**
を書いています。

## 1. プロジェクトの目的

[MarginX](https://github.com/Yamanashi-laboratory/MarginX)（C++）の Java 移植です。

**移植元は参照実装であり、数値的な挙動の正解です。** 挙動に迷いが出たら、まず C++ 版が
どうしているかを基準に判断してください。「Java として自然かどうか」は、数値的な一致より
優先されません。設計（レイヤ分割・命名・並行処理の方式）は Java 側の裁量ですが、
計算結果を変える変更は、意図的な修正（§4）でない限り許されません。

## 2. アーキテクチャの不変条件

- 依存方向は `presentation → application → domain ← infrastructure` の **一方向のみ**。
- `domain` は `java.nio.file` / `java.io` / picocli / その他の外部ライブラリを参照しない。
- ファイル・プロセス・外部コマンドなど外部技術へのアクセスは、必ず `domain/port` の
  インタフェース経由。実装は `infrastructure` に置く。
- `application` は手順の組み立てのみを行う。計算ロジックは `domain/service` に置く。

この規約は [LayerDependencyTest](src/test/java/com/ynu/marginx/architecture/LayerDependencyTest.java)
（ArchUnit）が機械的に強制します。**このテストを緩める変更（レイヤ定義の書き換え、禁止パッケージ
の削除、`@ArchTest` の無効化、除外ルールの追加）は禁止です。** テストが落ちたら、テストではなく
コードを直してください。新しい外部技術が必要になった場合も、例外を足すのではなく port を足します。

## 3. 意図的に保存している C++ 版の挙動（変更禁止・コメント必須）

以下は「バグに見えるが、結果が大きく変わるため元の挙動をそのまま保存している」箇所です。
リファクタリング中に善意で"直す"ことを禁止します。詳細は README
[「移植にあたり修正した元コードの不整合」](README.md#移植にあたり修正した元コードの不整合) 節を参照。

1. **小文字始まりの素子のみがマージン対象** — `L01` のような大文字始まりの行は対象外。
   元コードの `judge_element()` が小文字しか判定しない仕様で、事実上「対象マーカー」として
   機能しています（[NetlistParser](src/main/java/com/ynu/marginx/infrastructure/netlist/NetlistParser.java)）。
2. **シャント抵抗は素子の初期値から計算する** — 面積を掃引中でも、シャント抵抗は初期面積基準
   のまま（`make_cir.cpp` と同じ。
   [CircuitElement#renderShuntLine](src/main/java/com/ynu/marginx/domain/model/circuit/CircuitElement.java)）。
3. **CGM の `lambda` は整数除算のまま** — `(MULTI_NUM - success) / MULTI_NUM` は success が 1 以上なら
   常に 0 で、補正項が消えて「成功試行の重心」になります。これが手法名どおりの挙動なので、
   実数除算に直してはいけません
   （[CenterOfGravityOptimizer](src/main/java/com/ynu/marginx/domain/service/CenterOfGravityOptimizer.java)）。

この種のコードに触れるときは、**なぜその形なのかを説明するコメントを必ず残してください。**
既存のコメントを削らないでください。新たに同種の箇所を見つけたら、コメントを付けたうえで
このリストと README に追記します。

## 4. 意図的に修正した C++ 版の不整合（再導入禁止）

以下は移植時に**意図的に直した**箇所です。C++ 版と数値が食い違うのは想定どおりで、
「元に合わせる」方向の変更は禁止します（README
[同節](README.md#移植にあたり修正した元コードの不整合) と対応）。

1. **コンデンサの単位解析** — `sub_unit()` が基本単位の `F` をフェムト接頭辞 `f` として拾うため、
   C++ 版は `0.07pF` の単位を `fF` と誤認します。Java 版は基本単位を除いてから接頭辞を判定します。
2. **コンデンサの素子種別** — C++ 版は `case 4`（C）で `ide_num` に 5（R）を格納していました。
3. **電圧源の値** — C++ 版 `case 6` は消費済みの `stringstream` を再利用しており、PWL の途中の値を
   拾います。Java 版は電流源（`case 7`）と同じく、PWL 末尾の振幅を読みます。
4. **CGM の最良回路の初期値** — C++ 版は `best_value` を全 0 で開始するため、スコアを一度も更新
   できないと全パラメータ 0 の回路を返します。Java 版は入力回路を初期の最良として扱います。

golden ファイルと数値比較して差分が出たときは、まずこの 3 点に該当しないかを確認してください。

## 5. 作業の進め方

- **実装前に、変更するファイルと方針を箇条書きで提示し、承認を待つ。** 承認なしにコードを
  書き始めないこと。調査・読み取りは承認不要です。
- 変更後は必ず次を実行し、**テストが全通過すること**を確認する。

  ```bash
  ./gradlew build
  ```

  通らないまま「完了」と報告しないこと。落ちた場合は出力をそのまま提示します。
- **新しい振る舞いを追加したら必ずテストも追加する。** バグ修正なら、修正前に落ちる回帰テストを
  先に用意します。
- **未検証の前提や、確信が持てない箇所は README の該当節に明記する。** 「たぶん動く」を黙って
  残さないこと。README には既に未検証事項の記載箇所があるので、そこに揃えて追記します
  （例: 実 JoSIM の `.FILE` 挙動、ネイティブビルド）。
- **1 つの論理的な変更につき 1 コミット。** 複数の関心事（機能追加とリファクタリング、
  無関係なフォーマット修正など）を 1 コミットに混ぜない。コミットとプッシュは、ユーザーから
  指示があったときだけ行う。
